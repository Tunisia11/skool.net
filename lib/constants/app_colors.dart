import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors (Cyan/Blue like Taki Academy)
  static const Color primary = Color(0xFF2CBCE9); // Light Cyan
  static const Color primaryDark = Color(0xFF0099CC); // Darker Cyan for hovers/accents
  static const Color secondary = Color(0xFFFFC107); // Amber/Yellow for accents (like the bell/play button)
  
  // Backgrounds
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF8F9FA); // Very light grey for sections
  
  // Text
  static const Color textPrimary = Color(0xFF212529);
  static const Color textSecondary = Color(0xFF6C757D);
  static const Color textWhite = Color(0xFFFFFFFF);
  
  // UI Elements
  static const Color cardShadow = Color(0x1A000000);
}
